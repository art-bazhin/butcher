mixin categories
  section#categories.categories: div.categories__row
    each item in collections.categories
      div.categories__item
        if (item.data.category === category)
          span #{ item.data.title }
        else
          a(href=(item.url + '#categories')) #{ item.data.title }